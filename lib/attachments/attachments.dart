import 'package:flutter/material.dart';

// TODO IMPLEMENT

// Image & Video Gallery
// Attachement Tree
// --> Toggle Attached only (not inline image/video)\

// Admin override -> show everything (even private, not shared with admin account)
// Admin override -> delete

class AttachementsScrean extends StatelessWidget {
  const AttachementsScrean({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
