import 'package:flutter/material.dart';

// TODO IMPLEMENT

// Avatar
// Display Name
// Username
// optional Email
// show email on profile toggle

// Look and Feel
// themeing
// preferred language

// Security
// if auth via password
// --> password (require current toggle in settings)
// MFA (to be override by global admin settings)

// user roles (if has privileges)

// API -- if has privileges
// documentation link
// create API token

class ProfileEditorScrean extends StatelessWidget {
  const ProfileEditorScrean({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
