import 'package:flutter/material.dart';

// TODO IMPLEMENT

// Side Nav
// Creation/Update Details by User
// depending on screen size, push content to the left or show above content
// Actions (create new, view recently deleted)
// Recently Viewed Books
// Popular Books
// New Books

// Create New Button
// If on mobile create new (plus) button on bottom right

// Books Card Section
// Sort
// Book cards
// --> Book image, Parent Shelf, Title, Desc, create/updated relative

class BooksScrean extends StatelessWidget {
  const BooksScrean({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
